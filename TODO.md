3.16日问题
（已修复）1.仪表盘上方的：总内容数、待处理、已预审、已分析，显示都是0，这应该是故障
（已修复）2.点击一键运行流水线，提示错误
（已修复）3.采集智能体新增采集内容的时效限制：仅采集近三天的热点内容，tag按照"主题+分类"呈现，主题为：AI、安全、AI&安全，分类为：学术、工程，例如OPENCLAW相关话题，就是"AI+工程"
（已修复）4.内容库中的知识按栏目划分开，区分开已采纳(待分析)、已分析、已拒绝这样我就知道哪些知识需要等待，哪些知识现在可以阅读，哪些知识是低质量拒绝
（已修复）5.内容库中的某一个栏目下的知识按照列表形式排版，设计必要的字段，简单直观，现在的内容是方块，我觉得有些臃肿

3.17日问题
（已修复）1.仪表盘--采集内容：提示采集失败
    原因1：ECONNREFUSED 表示后端服务未启动或未就绪
    修复1：增加后端健康检查，等待后端就绪后再启动前端
    原因2：系统使用了 Python 2.7 而非虚拟环境中的 Python 3
    修复2：智能检测Python环境 - 优先使用虚拟环境，否则使用系统的python3/python命令
    原因3：Python依赖未安装（ModuleNotFoundError: No module named 'fastapi'）
    修复3：启动前检查依赖，未安装则提示自动安装
    测试：很好，顺利完成了

（已修复）2.start.bat启动后，输入选项后里面又弹出启动工具，不符合使用方式。应该使用1.运行服务后自动结束启动bat，保持另外两个服务窗口运行即可。选择2.修改配置，完成后自动回到初始界面
    测试：很好，顺利完成了

（已修复）3.metis的插画右侧框没有对齐，需要修改对齐
    测试：很好，顺利完成了

（已修复）4.发现问题，启动bat后，后端服务窗口似乎已经完成启动，但是启动bat窗口仍然持续等待
    原因：PowerShell 的 Invoke-WebRequest 健康检查失败
    修复：改用 curl 命令和 .Net WebClient 双重检查方式
    测试：很好，顺利完成了
            
（已修复）5.前端内容中：关于内容库，运行中、已采纳0、已分析20、已拒绝0、待处理0。可以看到，当我选中的时候数量显示正确，未点击选中栏目时，右侧数字为0。
    原因：只获取了当前标签页的内容，未获取各状态的数量统计
    修复：在组件加载时调用 dashboard API 获取各状态的数量
    测试：很好，顺利完成了

（已修复）6.仪表盘：首页的总内容数、待处理、已预审、已分析：显示全部都是0，这里应显示对应内容数量
    原因：Dashboard组件数据获取逻辑需要改进，缺少错误处理和空值处理
    修复：改进前端Dashboard.tsx的数据获取逻辑，添加更好的错误处理；改进后端dashboard API，添加日志和空值处理
    测试：很好，顺利完成了

（已修复）7.快捷操作中：采集内容可以执行，预审、分析、完整流水线全部执行失败
    原因：预审和分析操作需要对每个内容调用LLM API，频繁调用触发429限流
    修复：在ReviewerAgent和AnalyzerAgent中添加API请求延迟（预审2秒，分析3秒），防止触发限流
    说明：采集智能体只在分类新闻内容时调用API，调用频率低；预审和分析每个内容都需要调用
    测试：待验证（API问题待解决）

（已修复）8.现在的待处理命名不够准确，修改成待预审或者待分析，明确处于哪个节点。
    修复：将"待处理"改为"待预审"，Dashboard和Contents页面均已更新
    说明：流程是 待预审(pending) -> 已采纳/待分析(reviewed) -> 已分析(analyzed)
          "已采纳"标签就是"待分析"的内容，预审通过后进入已采纳队列等待分析

（已修复）9.使用快捷操作时，采集就完成采集，预审评分应该对采集队列中剩余未完成评分的继续评分
    原因：API的content_id改为可选后，不传递时会处理队列中的待处理内容
    修复：已修复API，预审和分析操作会自动处理队列中的待处理内容
    测试：待验证（API问题待解决）

（已修复）10.首页的最近采集，没有显示内容，实际内容库中有近期数据，这块逻辑有问题
    原因：前端数据处理逻辑不够健壮，后端API缺少日志和错误处理
    修复：改进后端dashboard API添加日志、空值处理和更好的排序；改进前端Dashboard.tsx的数据处理
    测试：很好，顺利完成了

（已修复）11.现在的采集内容质量不高，我需要你调整采集源，论文：arXiv、OpenReview、Google Scholar、Journal of Machine Learning
   Research、IEEE Transactions on Pattern Analysis and Machine Intelligence、Nature Machine
   Intelligence、Nature Communications等相关的CCF
   A/B类别会议及期刊论文。新闻：RadarAI（一站式工程聚合）、GitHub Trending + Hugging Face（开源 /
   模型源头）、OpenAI/Anthropic/Google/Meta/ 字节 / 阿里 / 百度官方博客（一手发布）、Papers with
   Code（论文 + 工程）、安全牛 / FreeBuf/Krebs on Security（AI
   安全工程）。针对采集源仍然需要筛选AI、安全、AI&安全领域，你可以使用关键词或者其他方式。
   中文内容就用中文分析总结，英文内容分析总结则给出双语对照。
   状态：（已修复）
   新增学术论文源：
     - OpenReview（AI/ML顶会论文）
     - Semantic Scholar（学术论文搜索）
     - Papers with Code（论文+工程实现）
   新增技术新闻源：
     - GitHub Trending（开源项目）
     - Hugging Face（模型/数据集）
   新增安全新闻源：
     - FreeBuf（网络安全）
     - 安全牛（安全资讯）
   新增AI公司博客：
     - OpenAI、Anthropic、Google AI
   双语对照支持：
     - 自动检测内容语言
     - 英文内容输出双语对照（英文+中文翻译）
   测试：待测试

（已修复）12.前端补充，内容的需要再加一项tag：文章来源
    修复：更新 Badge 组件，添加 source 类型，显示文章来源的友好名称；更新 ContentListItem 和 ContentCard 组件
    测试：待测试，因为目前还没有新来源的文章被采集到

（已修复）13.更新来源后，采集开始批量报错
    原因：datetime 时区问题 - 代码使用了 datetime.utcnow() 返回无时区的时间，而数据源返回的是带时区的时间
    修复：使用 datetime.now(timezone.utc) 获取带时区的当前时间，并确保 published_date 也带时区
    测试：很好，修复完成

（已修复）14.快捷操作中的预审评分操作，点击后调用API完成预审。后台日志显示比较慢的调用完成了预审，但是前端报错
    原因：前端 API 超时时间设置为30秒，而预审操作需要约45秒（处理12条内容）
    修复：
      1. 将长时间运行操作（采集、预审、分析、流水线）的超时时间增加到3分钟
      2. 改进快捷操作按钮，添加"请耐心等待..."提示文字
    测试：很好，修复完成

（已修复）15.根据实际使用，两周或一月采集一次信息，那么待预审文章一次性就会囤积很多。
    一旦服务出现问题，那么预审中断，那么已经完成预审的数据也会丢失，浪费时间浪费资源而且稳定性很差。
    在实际开发场景中，这种问题一般有什么好办法处理？
    答案：当前实现已经是增量保存的（每处理完一条内容就立即保存到数据库），中断不会丢失已处理的数据。
    改进：
      1. 添加进度追踪功能 - 预审和分析智能体都添加了进度状态
      2. 添加进度查询API - /api/review/progress 和 /api/analyze/progress
      3. 前端轮询进度API显示处理进度
      4. 将长时间操作超时时间增加到10分钟
      5. 改为后台任务模式 - API立即返回，后台执行任务，前端轮询进度
      6. 显示进度条和完成条数（通过/拒绝/成功/失败）
    测试：很好，修复完成

（已修复）16.同理在分析阶段和预审也有一样样的线性持续处理问题，如何解决
    答案：同问题15，已实现增量保存和进度追踪
    改进：
      1. 后台任务模式 + 进度轮询
    测试：很好，修复完成

（已修复）17.前端修改：学习记录下，分两个栏目，已学习和未学习和收藏（可以计数）。
    将所有已分析内容，按照得分高低顺序排列填入未学习栏目
    未学习目录中的每一篇内容都可以点开阅读分析内容、标记已读、收藏、及填写学习笔记
    标记已读后，该文章将从未学习转到已学习，标记收藏后，该文章则单独复制到收藏栏目下。
    修复：
      1. 添加三个标签页：未学习、已学习、收藏
      2. 每个标签页显示对应计数
      3. 优化卡片显示：分数显示、操作按钮
      4. 标记已读/收藏后自动刷新计数
      5. 修复后端API的total计数问题（之前返回当前页数量而非总数）
      6. 按分数降序排列学习记录
      7. 原地展开详情功能 - 点击展开显示完整分析内容
      8. 添加学习笔记编辑功能
      9. 显示知识关联、易混淆辨析、学习建议
    测试：很好，修复完成

（已修复）18.学习记录中未学习的知识记录在内容库的已分析中居然没有，数据是不是有问题？
    原因分析：
      1. 分析智能体创建学习记录时，如果预审记录(review)为None，则不会创建学习记录
      2. 但内容状态仍会更新为"analyzed"，导致数据不一致
      3. 内容库分页功能有问题：total返回的是当前页数据量而非总数
      4. 内容库搜索是在前端进行的，只搜索当前页内容，无法跨页搜索
    修复：
      1. 修改AnalyzerAgent，确保即使review为None也能创建默认review并创建学习记录
      2. 修复内容库分页：后端API返回正确的total总数，前端分页逻辑修正
      3. 添加后端搜索功能：内容库和学习中心都支持后端搜索，可以跨页搜索
      4. 修改"学习记录"名称为"学习中心"
      5. 修复学习中心数量显示和分页功能：显示总数、页码、跳转功能
      6. 添加user_notes字段到学习记录响应中
    清理：
      - 移除数据一致性检查和修复API（已验证数据一致性问题已解决）
      - 移除学习中心的"修复数据"按钮
    测试：修复完成

（已修复）19.前端的设置模块优化
    问题：
      - 系统状态中应用名称信息不对，应显示Metis和发布版本
      - 缺少可视化配置.env的功能
      - 定时任务调度器缺少说明
      - 数据源配置模块意义不明
      - 快捷操作与仪表盘重复
    修复：
      1. 系统状态：应用名称改为"Metis"，数据库显示类型和地址，LLM显示提供商和模型
      2. LLM配置：添加可视化编辑界面，支持编辑API Key、Base URL、模型名称、Temperature、及格分数、采集间隔
      3. 调度器：添加功能说明（自动采集→预审→分析流程）和当前配置参数展示
      4. 移除数据源配置模块
      5. 移除快捷操作模块
      6. 后端新增API：GET /settings（详细状态）、GET /settings/env（读取配置）、POST /settings/env（保存配置）
    测试：已修复

